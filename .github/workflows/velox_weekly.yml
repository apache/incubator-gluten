# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Velox backend weekly job

on:
  pull_request:
    paths:
      - '.github/workflows/velox_weekly.yml'
  schedule:
    - cron: '0 20 * * 0'

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
  CCACHE_DIR: "${{ github.workspace }}/.ccache"

concurrency:
  group: ${{ github.repository }}-${{ github.head_ref || github.sha }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-on-centos:
    strategy:
      fail-fast: false
      matrix:
        os: [ "centos:7", "centos:8", "quay.io/centos/centos:stream9" ]
    if: ${{ startsWith(github.repository, 'apache/') }}
    runs-on: ubuntu-22.04
    container: ${{ matrix.os }}
    steps:
      - name: Update mirror list
        run: |
          sed -i -e "s|mirrorlist=|#mirrorlist=|g" /etc/yum.repos.d/CentOS-* || true
          sed -i -e "s|#baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g" /etc/yum.repos.d/CentOS-* || true
      - name: build
        run: |
          yum update -y
          yum install -y epel-release sudo dnf
          if [ "${{ matrix.os }}" = "centos:7" ]; then
            yum install -y centos-release-scl
            rm /etc/yum.repos.d/CentOS-SCLo-scl.repo -f
            sed -i \
            -e 's/^mirrorlist/#mirrorlist/' \
            -e 's/^#baseurl/baseurl/' \
            -e 's/mirror\.centos\.org/vault.centos.org/' \
            /etc/yum.repos.d/CentOS-SCLo-scl-rh.repo
            yum install -y devtoolset-11
            source /opt/rh/devtoolset-11/enable
          elif [ "${{ matrix.os }}" = "quay.io/centos/centos:stream9" ]; then
            dnf install -y --setopt=install_weak_deps=False gcc-toolset-12
            source /opt/rh/gcc-toolset-12/enable || exit 1
          else
            dnf install -y --setopt=install_weak_deps=False gcc-toolset-11
            source /opt/rh/gcc-toolset-11/enable || exit 1
          fi
          yum install -y java-1.8.0-openjdk-devel patch wget git perl
          export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk && \
          export PATH=$JAVA_HOME/bin:$PATH
          wget --no-check-certificate https://downloads.apache.org/maven/maven-3/3.8.8/binaries/apache-maven-3.8.8-bin.tar.gz && \
          tar -xvf apache-maven-3.8.8-bin.tar.gz && \
          mv apache-maven-3.8.8 /usr/lib/maven && \
          export MAVEN_HOME=/usr/lib/maven && \
          export PATH=${PATH}:${MAVEN_HOME}/bin && \
          git clone -b main --depth=1 https://github.com/apache/incubator-gluten.git && cd incubator-gluten/
          ./dev/package.sh

  build-on-ubuntu:
    strategy:
      fail-fast: false
      matrix:
        os: [ "ubuntu:20.04", "ubuntu:22.04" ]
    if: ${{ startsWith(github.repository, 'apache/') }}
    runs-on: ubuntu-22.04
    container: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          # To avoid the prompt for region selection during installing tzdata.
          export DEBIAN_FRONTEND=noninteractive
          apt-get update && apt-get install -y sudo maven wget git
          if [ "${{ matrix.os }}" = "ubuntu:20.04" ]; then
            sudo apt install -y software-properties-common
            sudo add-apt-repository ppa:ubuntu-toolchain-r/test
            sudo apt update && sudo apt install -y gcc-11 g++-11
            export CC=/usr/bin/gcc-11
            export CXX=/usr/bin/g++-11
          fi
          sudo apt-get install -y openjdk-8-jdk
          export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
          cd $GITHUB_WORKSPACE/ && ./dev/package.sh

  spark-function-support-check:
    runs-on: ubuntu-22.04
    container: apache/gluten:centos-8-jdk8
    steps:
      - uses: actions/checkout@v2
      - name: Get Ccache
        uses: actions/cache/restore@v4
        with:
          path: '${{ env.CCACHE_DIR }}'
          key: ccache-centos8-release-default-${{github.sha}}
          restore-keys: |
            ccache-centos8-release-default
      - name: Build Gluten native libraries
        run: |
          df -a
          bash dev/ci-velox-buildshared-centos-8.sh
          ccache -s
      - name: Generate function support log
        run: |
          cd /opt &&
          wget -q https://archive.apache.org/dist/spark/spark-3.5.2/spark-3.5.2-bin-hadoop3.tgz &&
          tar xf spark-3.5.2-bin-hadoop3.tgz && rm spark-3.5.2-bin-hadoop3.tgz -f
          mv /opt/shims/spark35/spark_home/sql/ /opt/spark-3.5.2-bin-hadoop3/
          pip3 install findspark tabulate
          export SPARK_SCALA_VERSION=2.12
          cd $GITHUB_WORKSPACE
          mvn clean package -Pbackends-velox -Pspark-3.5 -DskipTests
          cd $GITHUB_WORKSPACE/tools/scripts
          python3 gen-function-support-docs.py --spark_home=/opt/spark-3.5.2-bin-hadoop3/
      - name: Upload test report
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}-report
          path: 'docs/velox-backend-*-function-support.md'
